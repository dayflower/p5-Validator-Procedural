# TODO

- Make ctor acceptable filter / checker / procedure classes
- Create Validator::Procedural::Filter::Common
- Create Validator::Procedural::Checker::Common
- Make remove_field() acceptable filtering subroutine
- Fix croak (level) in Validator::Procedural::Field
